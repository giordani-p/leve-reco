# ===========================================
# CONFIGURAÇÕES DO SISTEMA DE RECOMENDAÇÃO
# ===========================================
# Configurações essenciais para PostgreSQL + pgvector

# ===========================================
# CONFIGURAÇÕES DE BANCO (ESSENCIAIS)
# ===========================================
# Usar as mesmas variáveis do módulo principal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# URL completa (opcional - será construída automaticamente)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ===========================================
# CONFIGURAÇÕES AVANÇADAS (OPCIONAIS)
# ===========================================
# Apenas configurar se precisar ajustar valores padrão

# Connection pool (padrão: 10/20/30/3600)
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=3600

# Sincronização (padrão: 100/30/true)
# SYNC_BATCH_SIZE=100
# SYNC_INTERVAL_MINUTES=30
# SYNC_ENABLED=true

# Cache (padrão: 3600/1000)
# CACHE_TTL_SECONDS=3600
# CACHE_MAX_SIZE=1000

# Backend (padrão: true)
# USE_PERSISTENT_INDEX=true
