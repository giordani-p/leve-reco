# ===========================================
# CONFIGURAÇÕES DO SISTEMA DE RECOMENDAÇÃO
# ===========================================
# Configurações essenciais para PostgreSQL + pgvector

# ===========================================
# CONFIGURAÇÕES DE BANCO (ESSENCIAIS)
# ===========================================
# Usar as mesmas variáveis do módulo principal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432


# ===========================================
# CONFIGURAÇÕES AVANÇADAS (OPCIONAIS)
# ===========================================
# Apenas configurar se precisar ajustar valores padrão

# Connection pool (padrão: 10/20)
# DB_POOL_SIZE=10
# DB_MAX_OVERFLOW=20

# Sincronização (padrão: 100)
# SYNC_BATCH_SIZE=100

# Backend (padrão: true)
# USE_PERSISTENT_INDEX=true
